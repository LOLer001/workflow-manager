{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10,
            "statusMessage": "Restoring Workflow Manager task state"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10,
            "statusMessage": "Saving Workflow Manager checkpoint"
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$PLUGIN_ROOT\"; runner=\"$root/scripts/run_orchestrator_hook.sh\"; if [ ! -f \"$runner\" ]; then runner=\"\"; parent=\"$(dirname \"$root\")\"; for candidate in \"$parent\"/*/scripts/run_orchestrator_hook.sh; do [ -f \"$candidate\" ] || continue; if [ -z \"$runner\" ] || [ \"$candidate\" -nt \"$runner\" ]; then runner=\"$candidate\"; fi; done; fi; if [ -n \"$runner\" ]; then PLUGIN_ROOT=\"$(dirname \"$(dirname \"$runner\")\")\"; export PLUGIN_ROOT; sh \"$runner\"; fi",
            "commandWindows": "cmd.exe /d /c powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"$root=$env:PLUGIN_ROOT; $selectedRoot=$root; $runner=Join-Path $root 'scripts\\run_orchestrator_hook.ps1'; if(-not (Test-Path -LiteralPath $runner -PathType Leaf)){ $runner=$null; $latest=[DateTime]::MinValue; $parent=Split-Path -Parent $root; if(Test-Path -LiteralPath $parent -PathType Container){ foreach($directory in [IO.Directory]::EnumerateDirectories($parent)){ $candidate=Join-Path $directory 'scripts\\run_orchestrator_hook.ps1'; if(Test-Path -LiteralPath $candidate -PathType Leaf){ $modified=[IO.File]::GetLastWriteTimeUtc($candidate); if($modified -gt $latest){ $latest=$modified; $runner=$candidate } } } }; if($null -ne $runner){ $selectedRoot=Split-Path -Parent (Split-Path -Parent $runner) } }; if($null -ne $runner){ $env:PLUGIN_ROOT=$selectedRoot; & $runner }\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
