interface:
  display_name: "Workflow Manager"
  short_description: "Authorize Hard plans and verify runtime truth"
  default_prompt: "Use $workflow-manager only for Hard authorization, canonical contracts, runtime-truth evidence, mounted-tree safety, and active-contract continuity; let current Codex own ordinary execution while preserving acceptance evidence and compaction continuity."
policy:
  allow_implicit_invocation: true
