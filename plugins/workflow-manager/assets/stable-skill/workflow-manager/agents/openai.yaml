interface:
  display_name: "Workflow Manager"
  short_description: "Protect reasoning quality while managing context and agents"
  default_prompt: "Use $workflow-manager as a thin continuity and authorization layer: keep Simple and Focused work local, reserve highest-model assessment and strict confirmation for genuinely Hard work, preserve acceptance evidence and compaction continuity, and actively correct recoverable failures before reporting a blocker."
policy:
  allow_implicit_invocation: true
