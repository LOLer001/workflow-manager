interface:
  display_name: "Workflow Manager"
  short_description: "Authorize Hard plans and verify runtime truth"
  default_prompt: "Use $workflow-manager only for minimal Hard authorization, host-runtime truth, one-writer ownership, external safety, and active-contract continuity; let current Codex judge plans, task names, progress, recovery, and scheduling while preserving acceptance evidence."
policy:
  allow_implicit_invocation: true
